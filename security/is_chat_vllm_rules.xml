<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- L'admin IA voit tout -->
        <record id="is_chat_vllm_rule_admin" model="ir.rule">
            <field name="name">Chat VLLM : accès complet pour l'admin IA</field>
            <field name="model_id" ref="model_is_chat_vllm"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('group_ia_admin'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

    </data>
</odoo>
